/**
 * AURA - Doctor Settings
 * Placeholder: đổi mật khẩu, thông báo (sẽ cập nhật sau)
 */
(function () {
  'use strict';

  if (!window.AuraAuth || !window.AuraAuth.requireRole || !window.AuraAuth.requireRole('Doctor')) return;
})();
